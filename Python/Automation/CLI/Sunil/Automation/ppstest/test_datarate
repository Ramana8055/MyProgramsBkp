DATARATE=$1
iwpriv ath0 rate11a $DATARATE
GET_DATARATE=$(iwpriv ath0 get_rate11a | cut -d ":" -f 2)
if [ $DATARATE -eq $GET_DATARATE ]; then
    echo "datarate is set"
    datarate="datarate is set"
else
    echo "datarate is not set"
    datarate="datarate is not set"
fi

